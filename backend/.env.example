PORT=3000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:4200

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=pharmadash

# Si se define DATABASE_URL, tiene prioridad sobre las variables DB_* anteriores
DATABASE_URL=

JWT_SECRET=change-this-secret-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_DIAS=7
